name: Démo CI/CD de déploiement

# Se déclenche à chaque push sur main
on:
  push:
    branches: [ main ]

jobs:
  deploy-demo:
    runs-on: ubuntu-latest

    steps:
      # 1. Récupère le code du dépôt (indispensable)
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Exemple d'étape qui réussit toujours
      - name: Afficher un message de succès
        run: echo "🎉 Pipeline exécuté avec succès !"

      # 3. (optionnel) Vérifier la version de Node si tu en as besoin
      # - name: Setup Node
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: 18
      # - run: node --version